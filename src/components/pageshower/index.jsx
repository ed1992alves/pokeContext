import React from "react";
import { Link } from "react-router-dom";

const PageShower = (props) => {
    const {actualPage, totalPages}=props;

   

    return (
        <div>
            0/0
        </div>
    );
};

export default PageShower;